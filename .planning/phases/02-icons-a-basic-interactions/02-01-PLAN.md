---
phase: 02-icons-a-basic-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/tokens/icons.json
  - design-system/docs/icons.md
  - design-system/assets/icons/navigation/
  - design-system/assets/icons/actions/
  - design-system/assets/icons/status/
  - design-system/assets/icons/product/
autonomous: true

must_haves:
  truths:
    - "Designer can identify icon style specifications (2px stroke, rounded caps, 24x24 grid)"
    - "Designer can select correct icon size for context (16px inline, 20px buttons, 24px standalone)"
    - "Developer can access icon sizing tokens in CSS/JSON"
    - "Icon library selection (Lucide) is documented with usage examples"
    - "Developer can access 30+ optimized SVG icon files organized by category"
  artifacts:
    - path: "design-system/tokens/icons.json"
      provides: "Icon sizing and spacing tokens in W3C DTCG format"
      contains: "icon.size.xs"
    - path: "design-system/docs/icons.md"
      provides: "German icon documentation with style specs, sizing rules, and usage guidelines"
      min_lines: 150
    - path: "design-system/assets/icons/"
      provides: "Exported SVG icon files organized by category"
      min_files: 30
  key_links:
    - from: "design-system/tokens/icons.json"
      to: "design-system/tokens/spacing.json"
      via: "spacing token references"
      pattern: "\\{spacing\\."
---

<objective>
Create the icon system foundation with design tokens for icon sizing, export a base set of 30+ optimized Lucide icons as SVG files, and provide comprehensive German documentation covering icon style specifications, library integration, product-specific icon guidelines, and application rules.

Purpose: Establishes consistent icon visual language and provides designers/developers with clear specifications and ready-to-use icon assets for the design system.
Output: Icon tokens (JSON), exported icon files (SVG), and icon documentation (Markdown)
</objective>

<execution_context>
@/Users/frederichille/.claude/get-shit-done/workflows/execute-plan.md
@/Users/frederichille/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-icons-a-basic-interactions/02-CONTEXT.md
@.planning/phases/02-icons-a-basic-interactions/02-RESEARCH.md
@design-system/tokens/colors.json
@design-system/tokens/spacing.json
@design-system/tokens/effects.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create icon design tokens</name>
  <files>design-system/tokens/icons.json</files>
  <action>
Create icon tokens in W3C DTCG format ($value, $type syntax) covering:

**Icon Sizing Tokens:**
- icon.size.xs: 16px (minimum readable, compact UIs, inline with small text)
- icon.size.sm: 20px (default for buttons with text, inline icons)
- icon.size.md: 24px (standalone icons, base grid size, icon-only buttons)
- icon.size.lg: 32px (larger emphasis, hero sections)
- icon.size.xl: 48px (decorative use, maximum practical size)

**Icon Spacing Tokens:**
- icon.spacing.withText: reference {spacing.2} (8px gap between icon and text)
- icon.spacing.standalone: reference {spacing.1} (4px optical margin for alignment)

**Icon Style Tokens (documentation values):**
- icon.stroke.weight: 2px
- icon.stroke.cap: round
- icon.stroke.join: round
- icon.grid.size: 24px
- icon.grid.padding: 2px (1px visual padding on each side)

**Touch Target Tokens:**
- icon.touchTarget.minimum: 44px (WCAG recommended)
- icon.touchTarget.compact: 32px (desktop-only with spacing exceptions)

Use $description in German for all tokens. Reference Phase 1 spacing tokens where applicable.
  </action>
  <verify>
- File exists at design-system/tokens/icons.json
- JSON is valid (no syntax errors)
- All sizing tokens present (xs, sm, md, lg, xl)
- Spacing references use {spacing.N} syntax
- All descriptions in German
  </verify>
  <done>Icon tokens define 5 size levels, 2 spacing values, style specifications, and touch targets with German descriptions</done>
</task>

<task type="auto">
  <name>Task 2: Export base icon set as optimized SVG files</name>
  <files>design-system/assets/icons/navigation/, design-system/assets/icons/actions/, design-system/assets/icons/status/, design-system/assets/icons/product/</files>
  <action>
Export 35-40 Lucide icons as optimized SVG files organized by category. This delivers actual icon files (ICON-02) and export formats (ICON-04).

**Setup:**
1. Install dependencies: npm install lucide-static svgo --save-dev
2. Create directory structure:
   - design-system/assets/icons/navigation/
   - design-system/assets/icons/actions/
   - design-system/assets/icons/status/
   - design-system/assets/icons/product/

**Navigation Icons (10 icons):**
menu.svg, x.svg, chevron-down.svg, chevron-right.svg, chevron-left.svg, arrow-left.svg, arrow-right.svg, arrow-up.svg, arrow-down.svg, home.svg

**Actions Icons (12 icons):**
search.svg, filter.svg, download.svg, upload.svg, share.svg, edit.svg, trash-2.svg, plus.svg, minus.svg, copy.svg, check.svg, external-link.svg

**Status Icons (8 icons):**
check-circle.svg, alert-circle.svg, info.svg, alert-triangle.svg, loader.svg, x-circle.svg, bell.svg, clock.svg

**Product Icons (5 placeholders):**
Create placeholder icons for product-specific use:
droplet.svg, settings.svg, wrench.svg, package.svg, layers.svg

**For each icon:**
1. Copy from node_modules/lucide-static/icons/{name}.svg
2. Ensure viewBox="0 0 24 24"
3. Ensure stroke="currentColor" (not hardcoded color)
4. Ensure stroke-width="2"
5. Remove any fill attributes (should be none)

**Optimization with SVGO:**
Create svgo.config.js in design-system/:
```javascript
module.exports = {
  plugins: [
    'preset-default',
    'removeDimensions',
    { name: 'removeAttrs', params: { attrs: ['class', 'data-*'] } },
    { name: 'addAttributesToSVGElement', params: { attributes: [{ 'aria-hidden': 'true' }] } }
  ]
};
```

Run: npx svgo -f assets/icons --config svgo.config.js -r

**Optional PNG export (if requested later):**
Document in icons.md that PNG exports at 2x (48px) and 3x (72px) can be generated using:
npx svgexport {icon}.svg {icon}@2x.png 48:48
npx svgexport {icon}.svg {icon}@3x.png 72:72
  </action>
  <verify>
- Directory design-system/assets/icons/ exists with 4 subdirectories
- At least 35 SVG files present across categories
- Each SVG has viewBox="0 0 24 24"
- Each SVG uses stroke="currentColor" (grep for hardcoded colors should find none)
- Each SVG has stroke-width="2"
- SVGO config file exists at design-system/svgo.config.js
- SVG files are optimized (no unnecessary attributes, aria-hidden added)
  </verify>
  <done>35+ optimized SVG icons exported from Lucide, organized by category (navigation, actions, status, product) with standardized viewBox and currentColor stroke</done>
</task>

<task type="auto">
  <name>Task 3: Create icon documentation</name>
  <files>design-system/docs/icons.md</files>
  <action>
Create comprehensive German icon documentation (minimum 150 lines) covering:

**1. Icon-Stil (ICON-01)**
- Visual specifications: 2px stroke weight, rounded caps (border-radius: 2px), rounded joins
- Grid system: 24x24px base grid with 2px padding (20x20 safe area)
- Why these choices: Modern B2B aesthetic, consistency with Inter font curves, industry standard
- Example visual showing stroke weight and grid

**2. Icon-Bibliothek (ICON-02)**
- Primary library: Lucide Icons (ISC license, 2px customizable stroke)
- Installation: npm install lucide-react (or framework-agnostic SVG usage)
- Link to Lucide website: https://lucide.dev
- **Reference exported icon files in design-system/assets/icons/**
- List of included icons by category:

Navigation (assets/icons/navigation/):
- menu, x (close), chevron-down, chevron-right, chevron-left, arrow-left, arrow-right, arrow-up, arrow-down, home

Actions (assets/icons/actions/):
- search, filter, download, upload, share, edit, trash-2, plus, minus, copy, check, external-link

Status/Feedback (assets/icons/status/):
- check-circle, alert-circle, info, alert-triangle, loader, x-circle, bell, clock

Product-specific (assets/icons/product/):
- droplet, settings, wrench, package, layers (placeholders - custom icons follow same specs)
- Guidance for creating custom icons matching Lucide style

**3. Groessen und Anwendung (ICON-03)**
Table showing when to use each size:
| Groesse | Pixel | Anwendung | Beispiel |
|-------|-------|-----------|----------|
| XS | 16px | Inline mit kleinem Text, kompakte UIs | Breadcrumb-Chevrons |
| SM | 20px | Buttons mit Text, Formulare | Button "Speichern" |
| MD | 24px | Standalone-Icons, Icon-Buttons | Toolbar-Aktionen |
| LG | 32px | Hervorgehobene Aktionen | Feature-Highlights |
| XL | 48px | Dekorative Elemente | Leere-State-Illustrationen |

**4. Barrierefreiheit (WCAG AA)**
- aria-hidden="true" for decorative icons (accompanying text)
- aria-label on parent button for icon-only buttons
- role="img" + aria-label for informative icons
- Touch target minimum 44x44px for interactive icons
- Code examples for each pattern

**5. Export-Formate (ICON-04)**
- Primary: SVG with currentColor for fill/stroke (see assets/icons/)
- Fallback: PNG at 2x/3x for legacy systems (generation command documented)
- SVGO optimization settings in svgo.config.js
- viewBox standardization: "0 0 24 24"

**6. Don'ts**
- Nicht: Icons mit Text versehen (verhindert Lokalisierung)
- Nicht: Willkuerliche Groessen verwenden (17px, 22px)
- Nicht: Farbwerte hardcoden (immer currentColor)
- Nicht: Fokusindikatoren bei interaktiven Icons weglassen
  </action>
  <verify>
- File exists at design-system/docs/icons.md
- Document has sections for all ICON requirements (01-04)
- Lucide library referenced with installation instructions
- Icon categories with specific icon names listed
- **Reference to assets/icons/ directory with exported files**
- Size table with German headers present
- Accessibility section with ARIA patterns
- Minimum 150 lines of content
  </verify>
  <done>Icon documentation covers style specs (ICON-01), icon set with 35+ icons including exported files (ICON-02), sizing rules (ICON-03), and export formats with actual files (ICON-04) in German</done>
</task>

<task type="auto">
  <name>Task 4: Rebuild tokens and verify integration</name>
  <files>design-system/build/css/variables.css, design-system/build/json/tokens.json</files>
  <action>
Run Style Dictionary build to generate updated CSS and JSON outputs including new icon tokens:

1. Navigate to design-system directory
2. Run: npm run build
3. Verify icon tokens appear in build/css/variables.css with --icon- prefix
4. Verify icon tokens appear in build/json/tokens.json with resolved values

Expected CSS output should include:
- --icon-size-xs: 16px
- --icon-size-sm: 20px
- --icon-size-md: 24px
- --icon-size-lg: 32px
- --icon-size-xl: 48px
- --icon-spacing-with-text: var(--spacing-2) or 8px
- --icon-touch-target-minimum: 44px

If build fails, check that icons.json is valid JSON and follows W3C DTCG format.
  </action>
  <verify>
- npm run build completes successfully (exit code 0)
- build/css/variables.css contains --icon-size-xs, --icon-size-md, etc.
- build/json/tokens.json contains icon.size values
- **Verify icon.spacing.withText resolves spacing.2 reference correctly (8px in JSON output)**
- No new errors (warnings about typography composite types are expected from Phase 1)
  </verify>
  <done>Style Dictionary build includes icon tokens in CSS custom properties and JSON output with spacing references resolved correctly</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Icon tokens exist in tokens/icons.json with valid W3C DTCG format
2. **35+ SVG icon files exist in assets/icons/ organized by category**
3. Icon documentation exists in docs/icons.md with all required sections
4. Style Dictionary build succeeds and includes icon tokens
5. Token references to spacing.json resolve correctly (spacing.2 = 8px)
</verification>

<success_criteria>
- ICON-01 complete: Icon style defined (2px stroke, rounded caps, 24x24 grid)
- ICON-02 complete: Base icon set with 35+ exported SVG files (Lucide library + categories)
- ICON-03 complete: Icon application rules documented (size table, accessibility patterns)
- ICON-04 complete: Export formats delivered (optimized SVG files, PNG generation documented)
- All tokens available as CSS custom properties
- Spacing token references resolve correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-icons-a-basic-interactions/02-01-SUMMARY.md`
</output>
